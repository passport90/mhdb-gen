import type Controller from '../types/controller.js'

/**
 * Upserts events from frontmatter-bearing markdown files into the database.
 *
 * Stub — returns `0` until the parser, slug derivation, and repository layers land.
 *
 * @returns Exit code.
 */
const upsert: Controller = async () => 0

export default upsert
