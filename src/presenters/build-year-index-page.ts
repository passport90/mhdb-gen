/**
 * Applies the eta year-index template to its inputs. Hollow; takes the input opaquely via
 * `unknown` until the input type is designed.
 *
 * @param input - Year, seasons-with-events, and prev/next-year navigation; shape pending.
 * @returns Full HTML document for the year's `index.html` page.
 */
const buildYearIndexPage = (input: unknown): string => {
  void input

  throw new Error('buildYearIndexPage: not yet implemented')
}

export default buildYearIndexPage
