/**
 * Mirrors `src/assets/*` into `outputDir`, overwriting any out-of-date files and removing
 * orphans within the asset namespace only.
 *
 * @param outputDir - Output root.
 */
const syncStaticAssets = (outputDir: string): void => {
  void outputDir

  throw new Error('syncStaticAssets: not yet implemented')
}

export default syncStaticAssets
