/**
 * Slugifies the event title — NFKD-normalizes, strips non-ASCII, lowercases, drops non-word
 * punctuation, collapses runs of hyphens and whitespace to a single hyphen, trims leading and
 * trailing hyphens and underscores.
 *
 * @param _title - Event title.
 * @returns Slugified title.
 */
const slugify = (_title: string): string => ''

export default slugify
