/**
 * Derives a unique slug for an event title, appending `-2`, `-3`, ... on collision.
 *
 * @param _title - Event title.
 * @returns Slug unique within the events table.
 */
const deriveSlug = async (_title: string): Promise<string> => ''

export default deriveSlug
