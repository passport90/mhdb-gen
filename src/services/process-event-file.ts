/**
 * Persists the event described by the markdown file at the given path.
 *
 * @param _path - Path to the markdown file.
 */
const processEventFile = async (_path: string): Promise<void> => {}

export default processEventFile
